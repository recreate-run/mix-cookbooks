# Mix SDK Integration Examples

> **Add AI agents to your existing webapp in 30 lines of code**

## What This Is

These examples show how **easy** it is to integrate Mix SDK into your existing applications. We're not replacing your tools—we're showing you how to enhance them with AI capabilities.

## The Mix Integration Pattern

Every example follows this simple pattern:

```typescript
// 1. Initialize Mix client (1 line)
const mix = new Mix({ serverURL: "http://localhost:8088" })

// 2. Create a session (1 line)
const session = await mix.sessions.create({ title: "My Session" })

// 3. Send a message with streaming (5-10 lines)
await sendWithCallbacks(mix, session.id, "Your prompt here", {
  onContent: (text) => console.log(text),  // Handle streaming text
  onTool: (tool) => console.log(tool),      // Handle tool outputs
  onError: (error) => console.error(error)  // Handle errors
})
```

**That's it. 10-15 lines to add AI agents.**

---

## Examples

### 1. Portfolio Analysis 📊
**Add AI-powered CSV analysis to any dashboard**

**What it does:** Upload a CSV file, get AI analysis with auto-generated charts

**Integration points:**
- Financial dashboards
- Analytics platforms
- Admin panels
- Reporting tools

**Code complexity:** ~30 lines
**Time to integrate:** 1-2 hours

[View Example →](./python_examples/demos/portfolio_analysis.py) | [TypeScript →](./typescript_examples/demos/portfolio_analysis.ts)

**Key features:**
- File upload to Mix storage
- AI analyzes data automatically
- Generates visualizations
- Streams results in real-time

---

### 2. Image Analysis 🖼️
**Add AI image descriptions to photo galleries**

**What it does:** Upload an image, ask questions about it, get detailed analysis

**Integration points:**
- Photo galleries
- E-commerce product images
- Content moderation tools
- Medical imaging viewers

**Code complexity:** ~20 lines
**Time to integrate:** 30 minutes

[View Example →](./python_examples/demos/upload_image_ask.py) | [TypeScript →](./typescript_examples/demos/upload_image_ask.ts)

**Key features:**
- Image file upload
- Vision AI analysis (automatic model routing to Gemini)
- Natural language Q&A about images
- Multi-turn conversations

---

### 3. Web Search + Video Creation 🎬
**Add AI-powered video search and editing**

**What it does:** Search for videos, extract clips, create compilations

**Integration points:**
- Video editing apps
- Content creation tools
- Social media schedulers
- Marketing platforms

**Code complexity:** ~25 lines
**Time to integrate:** 1 hour

[View Example →](./python_examples/demos/tiktok_video.py) | [TypeScript →](./typescript_examples/demos/tiktok_video.ts)

**Key features:**
- Web search tool integration
- Video processing capabilities
- Automated editing workflows
- Export and download results

---

### 4. Multimodal Web Search 🔍
**Add AI-powered research to any app**

**What it does:** Search the web, find videos, extract key moments, download clips

**Integration points:**
- Research tools
- Content aggregators
- Knowledge bases
- Educational platforms

**Code complexity:** ~30 lines
**Time to integrate:** 1-2 hours

[View Example →](./python_examples/demos/web_search_multimodal.py) | [TypeScript →](./typescript_examples/demos/web_search_multimodal.ts)

**Key features:**
- Web search with Brave API
- Video analysis and clipping
- File downloads and storage
- Complex multi-step workflows

---

## Full Webapp Example: Portfolio Analyzer

We have a **complete TanStack Start application** showing Mix SDK integration in a production-ready webapp.

📁 **Location:** [`examples/portfolio-analyzer/`](./examples/portfolio-analyzer/)

**Features:**
- Drag-and-drop CSV upload
- Data preview table
- Real-time AI analysis with Server-Sent Events (SSE)
- Auto-generated visualizations
- Full-stack TypeScript

**What this shows:**
- How to structure a Mix-powered webapp
- Server-side Mix client setup
- SSE streaming implementation
- File upload handling
- Error management

[View Full App →](./examples/portfolio-analyzer/)

---

## How Easy Is It?

### Before: Your existing code
```typescript
// Your existing file upload handler
async function handleFileUpload(file: File) {
  const data = await parseCSV(file)
  displayData(data)
}
```

### After: With Mix SDK (just 10 lines added)
```typescript
import { Mix } from "mix-typescript-sdk"
import { sendWithCallbacks } from "./helpers"

const mix = new Mix({ serverURL: "http://localhost:8088" })

async function handleFileUpload(file: File) {
  const data = await parseCSV(file)
  displayData(data)

  // Add AI analysis (10 lines)
  const session = await mix.sessions.create({ title: "Analysis" })
  const uploaded = await mix.files.upload({
    id: session.id,
    requestBody: { file }
  })

  await sendWithCallbacks(mix, session.id,
    `Analyze this data: @${uploaded.url}`,
    {
      onContent: (text) => showAnalysis(text),
      onTool: (tool) => showVisualization(tool)
    }
  )
}
```

**That's it. Your file upload now has AI superpowers.**

---

## Quick Start

### Install Mix SDK

**Python:**
```bash
uv add mix-python-sdk
```

**TypeScript:**
```bash
bun add mix-typescript-sdk
```

### Run Mix Server

```bash
cd mix && make dev
```

Or use our startup script:
```bash
./start_mix.sh
```

### Try an Example

**Python:**
```bash
cd mix-cookbooks/python_examples
uv sync
uv run demos/portfolio_analysis.py
```

**TypeScript:**
```bash
cd mix-cookbooks/typescript_examples
bun install
bun run demo:portfolio
```

---

## Integration Patterns

### Pattern 1: CLI Tools
Add AI to command-line tools (see all demos/)

**Perfect for:**
- Data processing scripts
- Automation tools
- Developer utilities

### Pattern 2: Web Applications
Add AI to webapps (see examples/portfolio-analyzer/)

**Perfect for:**
- Dashboards
- Admin panels
- User-facing apps

### Pattern 3: API Endpoints
Wrap Mix in your API

**Perfect for:**
- Mobile app backends
- Third-party integrations
- Microservices

---

## What Makes Mix Easy?

### 1. Minimal Setup
```typescript
const mix = new Mix({ serverURL: "..." })  // Done
```

### 2. Automatic Model Routing
- **Gemini** for vision tasks
- **Claude** for reasoning
- **OpenAI** for search

You don't choose—Mix picks the best model automatically.

### 3. Built-in Streaming
```typescript
await sendWithCallbacks(mix, sessionId, prompt, {
  onContent: (text) => console.log(text)  // Streams automatically
})
```

### 4. File Upload Made Simple
```typescript
await mix.files.upload({ id: sessionId, requestBody: { file } })
```

### 5. Tool System Included
- Web search (Brave)
- Media analysis (images, videos, PDFs)
- Code execution (Bash)
- File operations (Read, Write, Edit)

All available out of the box.

---

## Ready to Integrate?

### For Python Developers
Start with: [`python_examples/demos/upload_image_ask.py`](./python_examples/demos/upload_image_ask.py)

**Why:** Simplest example, shows file upload + AI analysis in ~20 lines

### For TypeScript Developers
Start with: [`typescript_examples/demos/upload_image_ask.ts`](./typescript_examples/demos/upload_image_ask.ts)

**Why:** Same simple pattern, TypeScript syntax

### For Webapp Developers
Start with: [`examples/portfolio-analyzer/`](./examples/portfolio-analyzer/)

**Why:** Full production example with SSE streaming, file uploads, and visualizations

---

## Learn More

- [Mix Documentation](https://recreate.run/docs/mix)
- [Python SDK Reference](https://github.com/recreate-run/mix-python-sdk)
- [TypeScript SDK Reference](https://github.com/recreate-run/mix-typescript-sdk)
- [Mix Repository](https://github.com/recreate-run/mix)

---

## Questions?

- 💬 [Discord Community](https://discord.gg/mix)
- 🐙 [GitHub Issues](https://github.com/recreate-run/mix/issues)
- 📧 [Email Support](mailto:support@recreate.run)

---

**Remember:** We're not showing you how to build new products. We're showing you how to add AI to what you already have—in minutes, not days.
